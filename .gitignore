node_modules/
.DS_Store
workspace/scratch/
# Company data lives outside repo (symlinked from ~/Documents/GHQ/companies/)
# Track manifest.yaml but ignore individual company symlinks
companies/*
!companies/manifest.yaml

# Dolt database files (added by bd init)
.dolt/
*.db
.beads/dolt-server.activity
.beads/dolt-monitor.pid
.beads/dolt-server.port
/.claude/worktrees
loops/state.jsonl

# Scheduler runtime files (pid, lock, logs)
loops/agents/*.pid
loops/agents/*.lock
loops/agents/*.log
