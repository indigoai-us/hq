name: Publish to npm
on:
  push:
    tags: ["v*"]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - run: npm ci
      - run: npm run build --workspaces --if-present

      # Publish hq-cloud first (dependency of hq-cli)
      - run: npm publish --workspace packages/hq-cloud --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      # Publish hq-cli via npm trusted publishing (OIDC). No NODE_AUTH_TOKEN —
      # auth is short-lived, minted from GitHub OIDC token, and scoped to this
      # workflow file by npm's trusted-publisher config for @indigoai-us/hq-cli.
      - run: npm publish --workspace packages/hq-cli --access public --provenance

      # Publish create-hq
      - run: npm publish --workspace packages/create-hq --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
