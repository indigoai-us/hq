# ─────────────────────────────────────────────────────────────────────
# HQ Cloud E2E Test Environment Variables
# ─────────────────────────────────────────────────────────────────────
# Copy this file to .env.e2e and fill in real values.
# NEVER commit .env.e2e with actual credentials.
#
#   cp .env.e2e.example .env.e2e
#   # Edit .env.e2e with your test account credentials
#
# See e2e/README.md for full setup instructions.
# ─────────────────────────────────────────────────────────────────────

# ── Clerk keys (also needed in .env.local for the web app) ───────────
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
# CLERK_SECRET_KEY=sk_test_...

# ── User A: Primary test account ─────────────────────────────────────
# Used by: smoke tests, auth flow tests, single-user E2E tests
# Must exist in the Clerk dev instance with email+password auth enabled.
E2E_TEST_EMAIL=
E2E_TEST_PASSWORD=

# ── User B: Secondary test account (for multi-user isolation tests) ──
# Used by: cross-user isolation tests (E2E-FC-005), multi-user fixtures
# Must be a SEPARATE Clerk account with a DIFFERENT userId than user A.
# This ensures distinct S3 prefixes for file isolation verification.
#
# To create user B in Clerk:
#   1. Go to Clerk Dashboard > Users > Create User
#   2. Email: e2e-test-user-b@getindigo.ai (or similar)
#   3. Password: set a strong password
#   4. Copy the values here
E2E_TEST_EMAIL_B=
E2E_TEST_PASSWORD_B=
